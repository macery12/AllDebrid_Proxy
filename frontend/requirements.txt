# Frontend-only dependencies not already covered by the root requirements.txt.
# Jinja2 and Werkzeug are pinned here because they are transitive Flask deps
# whose versions matter for template rendering and streaming behaviour.
Jinja2==3.1.2
Werkzeug==3.0.4
# WSGI server for production – replaces the inline `pip install gunicorn` in Dockerfile.frontend
gunicorn==23.0.0
